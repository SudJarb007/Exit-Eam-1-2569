controller ที่ว่าจะมีหลักๆคือ

* ตอนลงคะแนน -> ลงในใบ เช้คว่า active ไหม, เคยลงมาก่อนยัง, election = open
* ตอนตรวจสอบ -> เพื่อปฏิเสธ + แต้งเหตุผล
   * case : เลือกซ้ำ
   * case : เลือกไม่ครบ
   * case : เคยลงคะแนน (มีแค่เคสนี้ที่กลับมาลงคะแนนเสียงต่อไม่ได้)
   * case : election status != open
* (R3)ตรวจหารูปแบบที่ซ้ำ + การปิดรับคะแนน
   * election = close
   * check : รูปแบบการจัดอันดับของบัตร
* ตรวจกลุ่มบัตร
   * ถ้าเจ้าหน้าที่ ตัดสิน -> รับรอง ไม่นับ รอเช้ค
   * if ใน กุ่มไม่นับ -> ทุกกุ่มจะเปลี่ยนตาม
* การสรุปผล
   * rank1 = 3, rank2 = 2, rank3 = 1
   * show total score ของ each ผุ้สมัคร
   * ตรวจสอบได้ว่า ->
      * บัตรเป็นของ voterคนไหน,
      * จัดอันดับผุ้สมัค (sorted ตาม score)
      * สถานะ
      * กุ้ท pattern -> ถูกตัดสินว่าอะไร